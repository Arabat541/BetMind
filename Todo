
## Amélioration des performances prédictives — classées par priorité


### ✅ A — Shots (HST/HS proxy xG) comme features — FAIT
### ✅ B — Walk-forward validation (TimeSeriesSplit n=5) — FAIT
### ✅ D — Hyperparameter tuning avec Optuna (40 trials) — FAIT
  Meilleurs params football : n_estimators=130, max_depth=3, lr=0.025
  Accuracy : 53.7% ± 0.9% (vs 52.4% avant shots) — sauvegardé best_params_football.json
### ✅ C — Ensemble XGB + LGB + stacking (LR méta-modèle) — FAIT
  Ensemble OOF accuracy : 53.8% ± 0.9% — sauvegardé football_ensemble_model.pkl
  predictor.py charge l'ensemble en priorité via model.py
### ✅ E — Fatigue / densité de calendrier — FAIT
### ✅ G — Feature "motivation" / enjeu du match — FAIT
### ✅ F — Marché BTTS (Both Teams To Score) — FAIT
### ✅ H — Signal cote d'ouverture vs cote actuelle (Sharp Money) — FAIT
### ✅ I — SHAP values feature importance — FAIT
### ✅ J — Données météo — FAIT
### ✅ K — Asian Handicap -0.5 — FAIT
### ✅ Dashboard v2.0 — FAIT
### ✅ L — Calibration isotonic sur l'EnsembleModel — FAIT
### ✅ N — Retrain automatique hebdomadaire (retrain.sh + cron) — FAIT
### ✅ O — Stop-loss dynamique par ligue — FAIT
### ✅ P — Backtesting interactif dans le dashboard — FAIT
### ✅ Q — Alertes Telegram enrichies (sharp money + météo) — FAIT
### ✅ M — Arbitrage / implied value multi-bookmakers — FAIT
### ✅ R — deploy.sh redémarre betmind + betmind-dashboard — FAIT
### ✅ S — ELO dynamique par équipe — FAIT
  home_elo, away_elo, elo_diff dans FEATURE_COLS + elo_ratings_current.json
### ✅ T — Suppression du vig (Shin method) — FAIT
  shin_probabilities() dans model.py — edge calculé vs vraie proba implicite
### ✅ U — Variance ensemble comme filtre de rejet — FAIT
  std > 0.08 entre XGB et LGB → skip ou réduction de mise
### ✅ V — CLV (Closing Line Value) comme KPI principal — FAIT
  KPI dashboard + alerte Telegram hebdo si CLV moyen < 0 sur 30 paris
### ✅ W — xG réel depuis Understat — FAIT
  understat_fetcher.py — 137 équipes, 21 414 entrées — rolling 8 matchs
  SHAP : home_xg_avg = 9ème feature la plus importante (0.0123)
### ✅ X — Feature arbitre — FAIT
  referee_avg_goals, referee_home_bias dans FEATURE_COLS
### ✅ Y — Valeur marchande effectifs (Transfermarkt) — FAIT
  transfermarkt_fetcher.py — 96 équipes — squad_value_ratio = 6ème SHAP (0.0257)
### ✅ Z — Corrélation entre marchés OU/BTTS — FAIT
  Over+BTTS Yes → +20% mise ; Over+No BTTS → les deux ignorés
### ✅ AA — Drawdown global stop-loss — FAIT
  Drawdown > 20% depuis peak → pause complète + alerte Telegram
### ✅ AB — Closing odds comme feature modèle — FAIT
  impl_cl_*, cl_move_* depuis BbClH/D/A dans les CSVs football-data
### ✅ AC — retrain.sh : refresh Understat + Transfermarkt avant retrain — FAIT
  Étape 0/3 : fetch xG + squad values avant chaque entraînement hebdo
### ✅ AD — xG features dans train_over_under.py + train_btts.py — FAIT
  6 features xG ajoutées aux deux modèles secondaires
### ✅ AE — Telegram : xG + squad_value_ratio dans alertes prédiction — FAIT
  Section xG récents (Understat) + ratio valeur effectifs dans send_prediction_alert()
### ✅ AF — Dashboard : widget xG et valeurs marchandes — FAIT
  Widget deux colonnes + endpoint /api/xg_squad + fetch parallèle dans loadData()


## 🟡 Priorité 3 — Infrastructure

### AG — MLflow model registry
  Historique des métriques à chaque retrain (accuracy, Brier, log-loss, CLV)
  Détection automatique de dégradation du modèle entre saisons
  UI légère sur le VPS

### AH — PostgreSQL + Redis
  SQLite → PostgreSQL pour concurrence (dashboard + predictor + result_checker)
  Redis pour cache des cotes (TTL 5min) → évite appels API répétés
